import { FC } from "react";

const Index: FC = () => {
	return (
		<section>
			<h1>End Screen!</h1>
			<button className="btn btn-dark">Play Again</button>
		</section>
	);
};

export default Index;
